export interface News {
  id: string;
  title: string;
  link: string;
  source: string;
  date: string;
  thumbnail: string; // base64 encoded
  snippet: string;
  iso_date: string;
  favicon: string; // base64 encoded
}

